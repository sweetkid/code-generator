package com.${org}.${app}.model.bo;


import com.${org}.Base;
import java.util.*;
import java.math.BigDecimal;
import com.${org}.${app}.model.entity.${entity};

/**
 * @author Quinn
 * @date ${.now?string("yyyy/MM/dd")}
 */
public class ${entity}Bo extends ${entity} {


}
